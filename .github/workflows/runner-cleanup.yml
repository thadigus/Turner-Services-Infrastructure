name: Runner Cleanup

on:
  workflow_dispatch:

jobs:
  reclaim-workspace-ownership:
    name: Reclaim Workspace Ownership
    runs-on: self-hosted
    container:
      image: alpine:3.20
      options: --user root
    steps:
      - name: Restore workspace ownership
        run: chown -R "$(stat -c '%u:%g' /__w)" "$GITHUB_WORKSPACE" || true
